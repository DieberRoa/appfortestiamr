pr:
  - main

variables:
- group: 'analyzepria-variables'

pool:
  vmImage: 'ubuntu-latest'

steps:
- script: |
    curl -X GET https://api-management-anayzeiapr.azure-api.net/fxanalyzeiapr/checkpr?mrnumber=$(System.PullRequest.PullRequestNumber) -H "api-key-analizeiapr: $(API_KEY_ANALIZEIAPR)"
  displayName: 'Check PR'